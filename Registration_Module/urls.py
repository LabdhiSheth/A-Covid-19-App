from django.contrib import admin
from django.urls import path,include
from . import views

urlpatterns = [
    # path('register/', views.register_request, name='registration-register'),
    # path('login/', views.login_request, name='registration-login'),
    # path('logout/', views.signout, name='registration-signout'),
]
